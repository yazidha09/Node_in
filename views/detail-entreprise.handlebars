<div class="max-w-4xl mx-auto bg-white p-8 rounded-lg shadow-md page-transition">
    {{#if entreprise}}
        <div class="flex flex-col md:flex-row gap-8 items-start">
            <div class="w-full md:w-64 flex-shrink-0">
                <div class="bg-gray-100 rounded-lg p-4 flex items-center justify-center h-48">
                    {{#if entreprise.logo}}
                        <img src="/uploads/logos/{{entreprise.logo}}" alt="Logo {{entreprise.nom}}" class="max-h-full max-w-full object-contain">
                    {{else}}
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-24 w-24 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
                        </svg>
                    {{/if}}
                </div>

                <div class="mt-4 space-y-3">
                    <a href="/demande-stage/{{entreprise.id}}" class="block w-full bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition text-center font-medium">
                        Demander un stage
                    </a>
                    
                    <a href="/liste-entreprises" class="block w-full border border-gray-300 text-gray-700 py-2 px-4 rounded-lg hover:bg-gray-50 transition text-center font-medium">
                        Retour à la liste
                    </a>
                </div>
            </div>
            
            <div class="flex-1">
                <div class="flex items-center justify-between mb-4">
                    <h1 class="text-2xl font-bold text-gray-800">{{entreprise.nom}}</h1>
                    <span class="px-3 py-1 bg-blue-100 text-blue-800 rounded-full text-sm">
                        {{entreprise.secteur}}
                    </span>
                </div>
                
                <div class="border-t border-b py-4 mb-4">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div>
                            <h3 class="text-sm font-medium text-gray-500">Adresse</h3>
                            <p class="mt-1 text-gray-800">{{entreprise.adresse}}</p>
                        </div>
                        
                        <div>
                            <h3 class="text-sm font-medium text-gray-500">Email</h3>
                            <p class="mt-1 text-gray-800">
                                <a href="mailto:{{entreprise.email}}" class="text-blue-600 hover:underline">{{entreprise.email}}</a>
                            </p>
                        </div>
                        
                        <div>
                            <h3 class="text-sm font-medium text-gray-500">Téléphone</h3>
                            <p class="mt-1 text-gray-800">
                                <a href="tel:{{entreprise.telephone}}" class="text-blue-600 hover:underline">{{entreprise.telephone}}</a>
                            </p>
                        </div>
                        
                        <div>
                            <h3 class="text-sm font-medium text-gray-500">Date d'inscription</h3>
                            <p class="mt-1 text-gray-800">{{formatDate entreprise.dateInscription}}</p>
                        </div>
                    </div>
                </div>
                
                <div class="mb-8">
                    <h2 class="text-lg font-semibold text-gray-800 mb-4">À propos de l'entreprise</h2>
                    <div class="text-gray-700 prose">
                        {{#if entreprise.description}}
                            <p>{{entreprise.description}}</p>
                        {{else}}
                            <p class="italic text-gray-500">Aucune description disponible pour cette entreprise.</p>
                        {{/if}}
                    </div>
                </div>
                
                <!-- Section pour les avis ou témoignages (futur développement) -->
                <div class="bg-blue-50 p-4 rounded-lg">
                    <div class="flex items-center">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-600 mr-2" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
                        </svg>
                        <p class="text-blue-700">Vous souhaitez faire un stage dans cette entreprise ? Cliquez sur "Demander un stage" pour envoyer votre candidature.</p>
                    </div>
                </div>
            </div>
        </div>
    {{else}}
        <div class="text-center py-8">
            <p class="text-red-500 text-lg mb-4">Entreprise non trouvée</p>
            <a href="/liste-entreprises" class="inline-block bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700">
                Retour à la liste des entreprises
            </a>
        </div>
    {{/if}}
</div>
